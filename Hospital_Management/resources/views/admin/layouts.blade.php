@include('admin.__partial.header')

            <!-- Begin Page Content -->
            <div class="container-fluid">
                @yield("content")
            </div>
            <!-- /.container-fluid -->
@include('admin.__partial.footer')
